/**
 * Notification Feature Routes
 *
 * Factory function for creating notification routes.
 * Called by the app shell to register client-side routes.
 */
export function createNotificationRoutes<T>(_parentRoute: T) {
  // Route configuration will be provided by the app shell
  return [] as const;
}
