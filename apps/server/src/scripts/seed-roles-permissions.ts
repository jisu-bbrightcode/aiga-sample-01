#!/usr/bin/env tsx

/**
 * Seed System Roles and Permissions
 *
 * This script requires @repo/features/role-permission to be installed.
 * It is a placeholder for the clean template.
 *
 * Usage (after role-permission feature is added):
 *   pnpm tsx src/scripts/seed-roles-permissions.ts
 */

// [ATLAS:SEED_IMPORT]
// import { NestFactory } from '@nestjs/core';
// import { AppModule } from '../app.module';
// import { RoleService, PermissionService } from '@repo/features/role-permission';
// [/ATLAS:SEED_IMPORT]

console.log("⚠️  role-permission feature is not installed. Skipping seed.");
console.log("   Install the feature first, then uncomment the imports above.");
